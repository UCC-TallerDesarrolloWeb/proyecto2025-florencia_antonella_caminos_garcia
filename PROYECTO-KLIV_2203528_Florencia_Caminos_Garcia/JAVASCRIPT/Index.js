document.addEventListener("DOMContentLoaded", function() {
    inicializeApp();
    setupHeaderEvents();
    setupSidebarEvents();
    setupDashboardEvents();
    setupProjectsEvents(); 
    setupTasksEvents(); 
    setupSettingsEvents(); 
    setupHelpEvents(); 
    setupGlobalEvents();
});